// The A2A (Agent-to-Agent) protocol — Koog 1.1.1 `a2a-core` + `a2a-client`. The message/task
// exchange model with its JSON wire form, and a JSON-RPC 2.0 client over an injectable synchronous
// transport. Pure and synchronous, so every target.

import {
  "moonbitlang/core/json",
  "moonbitstack/moonhttp/sse",
}

import {
  "moonbitlang/core/json",
  "moonbitstack/moonhttp/sse",
} for "test"
