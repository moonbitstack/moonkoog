// Agent checkpoint persistence (← Koog agents-features-snapshot): snapshot an
// agent's session — its conversation, LLM configuration, storage and strategy
// position — and restore it through a storage provider, with an in-memory
// implementation. Transcribed from the koog AgentCheckpointData +
// PersistenceStorageProvider source. Pure logic, all backends.
import {
  "moonbitstack/moonkoog/prompt",
  "moonbitstack/moonkoog/llm",
}
