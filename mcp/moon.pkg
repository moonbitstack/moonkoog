// The MCP (Model Context Protocol) adapter — Koog 1.1.1 `agents-mcp`. A JSON-RPC 2.0 client over
// an injectable synchronous transport that discovers an MCP server's tools and adapts each into a
// moonkoog `Tool`. Pure and synchronous, so every target.

import {
  "moonbitstack/moonkoog/tools",
}

import {
  "moonbitstack/moonkoog/tools",
  "moonbitlang/core/json",
} for "test"
